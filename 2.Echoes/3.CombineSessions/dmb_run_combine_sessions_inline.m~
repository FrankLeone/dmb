function out = dmb_run_combine_sessions_inline(job)

sessions = job.sess;
no_sessions = length(sessions);

total_data = [];
if ~isstr(sessions)
    for nr_sess = 1: no_sessions    
        total_data = [total_data(:); sessions{nr_sess}];
    end
else
    fieldNames = fieldnames(sessions(1));
    totalData = str
    for nr_sess = 1: no_sessions    
    

out(1).data = data;
out(2).no_sessions = no_sessions;